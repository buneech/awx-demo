---
- hosts: all
  become: yes

  tasks:
    - name: Create user "{{ user }}"
      user: 
        name: "{{ name }}"
        password: "{{ password }}"
        groups: "{{ group }}"
        createhome: yes
